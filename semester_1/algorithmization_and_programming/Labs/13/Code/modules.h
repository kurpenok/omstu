#ifndef CODE_MODULES_H
#define CODE_MODULES_H

void input(int*& array, int& size);

int max(int*& array, int& size);

int min(int*& array, int& size);

int* sum(int*& array_1, int*& array_2, int& size);

#endif
